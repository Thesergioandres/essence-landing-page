# nginx.conf ejemplo básico para servir una app React
server {
    listen 80;
    server_name www.sistemaERP.com sistemaERP.com;
    root /usr/share/nginx/html;

    location / {
        try_files $uri /index.html;
    }
}
