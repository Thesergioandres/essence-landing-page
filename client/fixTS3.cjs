const fs = require('fs'); const path = require('path'); function walk(d, cb){ fs.readdirSync(d).forEach(f=>{ let p = path.join(d, f); if(fs.statSync(p).isDirectory()) walk(p, cb); else cb(p); }); } let mod = 0; walk('./src', p=>{ if(!p.endsWith('.tsx') && !p.endsWith('.ts')) return; const org = fs.readFileSync(p, 'utf8'); let c = org; c = c.replace(/visibleEmpleados/ig, 'visibleEmployees').replace(/ProfitHistoryAdminEmpleado/ig, 'ProfitHistoryAdminEmployee').replace(/selectedEmpleado/ig, 'selectedEmployee').replace(/hasEmpleados/ig, 'hasEmployees').replace(/validEmpleados/ig, 'validEmployees').replace(/addEmpleado/ig, 'addEmployee').replace(/loadingEmpleados/ig, 'loadingEmployees').replace(/updateEmpleado/ig, 'updateEmployee').replace(/removeEmpleado/ig, 'removeEmployee').replace(/employees/g, 'employees'); if(c !== org){ fs.writeFileSync(p, c); mod++; console.log('Fixed', p); } }); console.log('Total fixed:', mod);
